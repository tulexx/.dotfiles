#!/usr/bin/env bash

# ignoreDirs=(
#   '\/.git'
#   '\/stow'
#   '\/\.idea\/'
# ) 
#
# for dir in ${ignoreDirs[@]}
# do
#   ignore="$ignore/$dir/d;"
# done
#
# find $HOME/.dotfiles -type f | sed $ignore | fzf | xargs -r -o editor

source "$HOME/.local/bashrc.d/dotfiles.sh"

dotfiles ls-tree --full-tree -r HEAD | awk '{ print $4 }' | fzf | xargs -I{} -r -o editor "$HOME/{}"
